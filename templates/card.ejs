<mat-card
    [magic]="<%= magic %>"
    <% if (attrVisible) { %>[style.visibility]="<%= attrVisible %>"<% } %>
    <% if (attrTooltip) { %>[matTooltip]="<%= attrTooltip %>"<% } %>
>
    <mat-card-content>
        <%- content %>
    </mat-card-content>
</mat-card>